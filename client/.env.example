# Backend API base URL — set this in your Netlify environment variables
# For local development, leave unset (Vite proxy handles /api → localhost:5000)
VITE_API_URL=https://team-srishti.onrender.com/api
